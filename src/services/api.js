// import axios from 'axios';

// const API_BASE_URL = 'https://jsonplaceholder.typicode.com/todos?_limit=6';

// export const getTasks = () => {
//   return axios.get(API_BASE_URL);
// };

// export const addTask = (title) => {
//   const newTask = {
//     title,
//     completed: false,
//   };
//   return axios.post(API_BASE_URL, newTask);
// };

// export const deleteTaskFromAPI = (taskId) => {
//   return axios.delete(`${API_BASE_URL}/${taskId}`);
// };

// export const updateTask = (taskId, updatedTask) => {
//   return axios.put(`${API_BASE_URL}/${taskId}`, updatedTask);
// };
